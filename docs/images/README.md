# Screenshots

The README links to these four files. Capture them at a viewport around 1440px wide, in light theme,
with browser chrome cropped out.

| File | What to capture |
|---|---|
| `01-updates-list.png` | **Store Updates** with the list populated, a level filter active and two or three rows selected so the selection bar shows. |
| `02-review.png` | **Review updates** with three or four applications, one version picker open. |
| `03-installer.png` | **Installer** mid-run — a mix of Installed, Installing and Not applied rows, so the reason column has something in it. |
| `04-diagnostics.png` | **Diagnostics** after a clean run. |

Crop out the instance name if you would rather it not be in a public repo.
